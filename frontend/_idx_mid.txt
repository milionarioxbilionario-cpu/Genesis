  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
}

.role-tab.selected {
  background: #dfe7ef;
  color: #111c2b;
  box-shadow: 0 1px 0 rgba(255,255,255,0.7);
}

.auth-error {
  border-radius: 12px;
  border: 1px solid rgba(239, 68, 68, 0.35);
  background: rgba(239, 68, 68, 0.1);
  color: #fecaca;
  padding: 10px 12px;
  font-size: 0.9rem;
}

.field-label {
  color: #dfe7ef;
  font-size: 0.94rem;
  font-weight: 500;
}

.auth-input {
  width: 100%;
  border-radius: 12px;
  background: rgba(17, 28, 43, 0.72);
  border: 1px solid rgba(138, 155, 175, 0.26);
  color: #f5f7fb;
  padding: 14px 14px 13px;
  font-size: 1rem;
  outline: none;
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.auth-input::placeholder {
  color: #8da0b7;
}

.auth-input:focus {
  border-color: rgba(122, 165, 214, 0.9);
  box-shadow: 0 0 0 3px rgba(122,165,214,0.15);
}

.auth-submit {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  width: 100%;
  border: none;
  border-radius: 12px;
  background: #dfe7ef;
  color: #111c2b;
  padding: 14px 16px;
  font-size: 1rem;
  font-weight: 700;
  cursor: pointer;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.85);
}

.auth-submit svg {
  width: 18px;
  height: 18px;
  stroke: currentColor;
}

.text-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  font-size: 0.95rem;
  font-weight: 500;
}

.text-link,
.company-link {
  color: #dfe7ef;
  text-decoration: none;
}

.text-link:hover,
.company-link:hover {
  color: #f1f5f9;
}

.company-link {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-radius: 12px;
  background: rgba(27, 36, 51, 0.9);
  border: 1px solid rgba(138, 155, 175, 0.26);
  padding: 14px 16px;
  font-size: 1rem;
  font-weight: 500;
}

.company-link svg {
  width: 18px;
  height: 18px;
  stroke: currentColor;
}

@media (max-width: 480px) {
  .auth-screen {
    padding: 26px 18px;
  }

  .auth-title {
    font-size: 2rem;
  }

  .role-tab {
    font-size: 0.85rem;
  }
}

.pos-shell {
  width: 100%;
  min-height: calc(100vh - 70px);
  background: radial-gradient(circle at 30% -10%, rgba(229,9,20,0.07), transparent 45%), var(--bg-base);
  color: var(--text);
  padding: 16px 18px 24px;
}

.pos-header-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  border-bottom: 1px solid var(--border);
  padding: 0 0 14px;
}

.pos-searchbar {
  display: flex;
  align-items: center;
  gap: 10px;
  width: min(100%, 620px);
  background: var(--surface);
  border: 1px solid var(--border-strong);
  border-radius: 12px;
  padding: 9px 12px;
  color: var(--text-dim);
}

.pos-searchbar svg {
  width: 16px;
  height: 16px;
  stroke: currentColor;
}

.pos-searchbar input {
  background: transparent;
  border: none;
  color: var(--text);
  flex: 1;
  outline: none;
  font-size: 0.96rem;
}

.pos-searchbar input::placeholder {
  color: var(--text-dim);
}

.shortcut {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 34px;
  height: 24px;
  border-radius: 6px;
  font-size: 0.74rem;
  font-weight: 600;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(148,163,184,0.18);
  color: #b9c4d6;
}

.pos-top-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.demo-chip {
  border: 1px solid rgba(148,163,184,0.18);
  background: rgba(17, 28, 43, 0.75);
  color: #c8d6e7;
  border-radius: 999px;
  padding: 7px 12px;
  font-size: 0.74rem;
  cursor: pointer;
}

.user-pill {
  width: 26px;
  height: 26px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #dfe7ef 0%, #7ca3d6 100%);
  color: #111c2b;
  font-size: 0.7rem;
  font-weight: 700;
}

.user-name {
  font-size: 0.85rem;
  color: #e4ebf5;
}

.pos-page-head {
  display: flex;
  align-items: end;
  justify-content: space-between;
  margin-top: 22px;
  gap: 16px;
}

.pos-page-head h1 {
  margin: 0;
  color: #edf2f7;
  font-size: clamp(2.1rem, 3vw, 2.7rem);
  letter-spacing: -0.05em;
  font-weight: 800;
}

.pos-page-head p {
  margin: 4px 0 0;
  color: rgba(191, 209, 230, 0.82);
  font-size: 1.05rem;
}

.cashier-close-shift {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: 1px solid rgba(148,163,184,0.18);
  background: rgba(19, 31, 47, 0.7);
  color: #eaf0f8;
  border-radius: 10px;
  padding: 10px 14px;
  cursor: pointer;
  font-weight: 600;
}

.cashier-close-shift svg {
  width: 16px;
  height: 16px;
  stroke: currentColor;
}

.pos-message {
  margin-top: 18px;
  border: 1px solid rgba(245, 158, 11, 0.35);
  background: rgba(245, 158, 11, 0.08);
  color: #f7d7a5;
  padding: 10px 12px;
  border-radius: 10px;
  font-size: 0.9rem;
}

.shift-modal {
  margin-top: 18px;
  border: 1px solid rgba(148,163,184,0.18);
  background: rgba(17, 28, 43, 0.72);
  border-radius: 16px;
  padding: 16px;
}

.shift-form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.shift-field {
  display: flex;
  flex-direction: column;
  gap: 8px;
  color: #dfe7ef;
  font-size: 0.9rem;
}

.shift-field input {
  width: 100%;
  border: 1px solid rgba(148,163,184,0.18);
  background: rgba(12, 22, 36, 0.9);
  color: #edf2f7;
  border-radius: 10px;
  padding: 10px 12px;
  outline: none;
}

.shift-actions {
  display: flex;
  justify-content: flex-end;
  gap: 12px;
  margin-top: 16px;
}

.secondary-btn,
.primary-btn {
  border: none;
  border-radius: 10px;
  padding: 10px 14px;
  font-weight: 600;
  cursor: pointer;
}

.secondary-btn {
  background: rgba(255,255,255,0.04);
  color: #dfe7ef;
  border: 1px solid rgba(148,163,184,0.18);
}

.primary-btn {
  background: #dfe7ef;
  color: #111c2b;
}

.pos-layout {
  display: grid;
  grid-template-columns: minmax(0, 1.4fr) minmax(300px, 0.75fr);
  gap: 20px;
  margin-top: 18px;
}

.product-panel,
.cart-panel {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 18px;
  padding: 16px;
}

.product-toolbar {
  margin-bottom: 16px;
}

.pill-group {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.pill {
  border: 1px solid rgba(148,163,184,0.18);
  background: rgba(255,255,255,0.02);
  color: #bfcfe2;
  border-radius: 10px;
  padding: 8px 12px;
  font-size: 0.82rem;
  cursor: pointer;
}

.pill.active {
  background: rgba(223,231,239,0.92);
  color: #111c2b;
  border-color: transparent;
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12px;
}

.product-card {
  position: relative;
  text-align: left;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 16px 14px 12px;
  color: var(--text);
  min-height: 130px;
  cursor: pointer;
  transition: transform 0.12s ease, border-color 0.12s ease;
}

.product-card:hover {
  transform: translateY(-1px);
  border-color: rgba(229,9,20,0.55);
}

.product-card-head {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 8px;
  margin-bottom: 18px;
}

.product-name {
  font-size: 1.02rem;
  font-weight: 600;
  line-height: 1.2;
  letter-spacing: -0.02em;
  color: #edf2f7;
}

.product-plus {
  width: 26px;
  height: 26px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255,255,255,0.08);
  font-size: 1.4rem;
  line-height: 1;
  color: #edf2f7;
}

.product-meta {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 8px;
}

.product-price {
  font-size: 1.1rem;
  color: #edf2f7;
  font-weight: 700;
}

.product-stock {
  font-size: 0.72rem;
  color: #aebed2;
}

.product-rank {
  position: absolute;
  right: 12px;
  bottom: 12px;
  font-size: 0.7rem;
  color: #9aa9bc;
}

.empty-products,
.empty-cart {
  border: 1px dashed rgba(148,163,184,0.22);
  background: rgba(255,255,255,0.03);
  color: #b9c4d6;
  border-radius: 12px;
  padding: 18px 14px;
  text-align: center;
}

.cart-panel {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.cart-title {
  font-size: 1.05rem;
  font-weight: 700;
  color: #edf2f7;
  margin-bottom: 4px;
}

.cart-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.cart-item {
  border: 1px solid rgba(148,163,184,0.14);
  background: rgba(12, 22, 36, 0.7);
  border-radius: 12px;
  padding: 12px;
}

.cart-item-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

.cart-item-name {
  font-size: 0.96rem;
  font-weight: 600;
  color: #edf2f7;
}

.cart-item-unit {
  font-size: 0.76rem;
  color: #9aa9bc;
  margin-top: 2px;
}

.remove-item {
  background: transparent;
  border: none;
  color: #fca5a5;
  font-size: 0.72rem;
  cursor: pointer;
}

.cart-item-controls {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  margin-top: 12px;
}

.quantity-box {
  display: inline-flex;
  align-items: center;
  border: 1px solid rgba(148,163,184,0.18);
  border-radius: 10px;
  overflow: hidden;
}

.quantity-box button {
  border: none;
  background: transparent;
  color: #edf2f7;
  width: 28px;
  height: 30px;
  cursor: pointer;
}

.quantity-box span {
  min-width: 26px;
  text-align: center;
  color: #edf2f7;
  font-weight: 600;
}

.cart-item-total {
  font-weight: 700;
  color: #edf2f7;
}

.totals-box,
.payment-box {
  border-top: 1px solid rgba(148,163,184,0.12);
  padding-top: 12px;
}

.total-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  color: #dfe7ef;
  font-size: 0.98rem;
  margin-top: 8px;
}

.total-row strong {
  font-weight: 700;
  color: #edf2f7;
}

.total-highlight {
  margin-top: 10px;
  font-size: 1.2rem;
  font-weight: 700;
}

.payment-label {
  display: flex;
  flex-direction: column;
  gap: 8px;
  font-size: 0.9rem;
  color: #dfe7ef;
}

.payment-methods {
  display: grid;
  grid-template-columns: repeat(3, minmax(0,1fr));
  gap: 8px;
}

.method-btn {
  border: 1px solid rgba(148,163,184,0.18);
  background: rgba(255,255,255,0.04);
  color: #dfe7ef;
  border-radius: 10px;
  padding: 8px 10px;
  cursor: pointer;
}

.method-btn.selected {
