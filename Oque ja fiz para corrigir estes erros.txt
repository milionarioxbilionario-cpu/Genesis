Resumo do que foi validado até ao momento:

1. Autenticação e sessão
- Confirmei que o backend expõe o endpoint /api/auth/me.
- Sem cookie de sessão, a rota responde 401 Unauthorized.
- Com login válido do owner, o endpoint devolve os dados do utilizador autenticado.
- A autenticação via cookie httpOnly e JWT foi verificada com login real.

2. ProtectedRoute e redirecionamentos por role
- O frontend contém ProtectedRoute e protege owner/cashier.
- Acesso sem autenticação redireciona para /login.
- O admin principal foi reduzido a redireção para /login para evitar exposição do painel no frontend principal.

3. Separação do painel admin
- Verifiquei a presença do middleware backend adminOriginCheck.
- /api/admin rejeita origin inválida com 403.
- A origem autorizada no ambiente de desenvolvimento (http://localhost:5175) passa a validação de origin e exige autenticação.

4. QR code e recibos
- Os recibos geram QR code com a URL verificável /verify/[sale_id].
- A lógica foi validada no ficheiro frontend/src/utils/receiptPrinter.js.

5. Build e compilação
- Validação bem-sucedida:
  - npm run build no frontend
  - npm run build no admin-frontend
  - node --check no backend/src/index.js

6. Smoke tests de API
- GET / -> 200 OK
- GET /api/auth/me -> 401 quando não autenticado
- GET /api/admin sem origem correta -> 403 Forbidden
- GET /api/admin com origem autorizada -> 401 por ausência de token, confirmando que origin check e autenticação estão operando em conjunto.

Conclusão:
As correções críticas listadas no plano foram implementadas e validadas com smoke tests. Há ainda pontos do roadmap que exigem implementação mais aprofundada, mas o conjunto principal de segurança/autenticação, onboarding e isolamento admin foi confirmado.

Atualização adicional (segunda etapa de execução):
- Reestruturei a navegação do Owner para dar acesso directo a módulos específicos dentro do Dashboard (produtos, stock, fornecedores, caixistas, trabalhadores, chenecas, metas e definições).
- Criei páginas modulares em frontend/src/pages/Owner/Goals.jsx, Settings.jsx e Debts.jsx para cobrir lacunas que estavam dispersas no dashboard monolítico.
- Integrei rotas dedicadas em frontend/src/App.jsx para /owner/products, /owner/stock, /owner/suppliers, /owner/cashiers, /owner/employees, /owner/debts, /owner/goals, /owner/settings e /owner/reports.
- Validado com builds de produção do frontend principal e do admin-frontend com sucesso.
- O objetivo desta melhoria foi reduzir a dependência do dashboard monolítico e aumentar a navegabilidade funcional do sistema sem quebrar o fluxo principal de autenticação e autorização.

Atualização final de acabamento:
- Corrigi o cálculo de lucro líquido do relatório mensal para refletir margem bruta menos despesas operacionais, em vez de uma expressão matemática que anulava a lógica de lucro.
- Adicionei persistência simples de horário de funcionamento por tenant no backend, com leitura e escrita via /api/owner/settings/hours.
- O frontend Settings agora carrega o horário guardado ao abrir a página, mantendo o valor atualizado no formulário.
- Revalidado em produção: frontend e admin-frontend compilam com sucesso; o backend continua a responder corretamente aos smoke tests de autenticação e origem admin.

================================================================================
ACTUALIZAÇÃO DE 18 DE SETEMBRO DE 2026 — Limpeza de segurança no GitHub
================================================================================

O que descobri (e que ninguém tinha registado):
- Enquanto eu estava a analisar o projecto, outro agente de IA (Copilot) fez dois
  commits e enviou-os para o GitHub sem o teu conhecimento: "Melhoria na
  arquitetura" e "Prompt". Esses commits levaram para o GitHub coisas que nunca
  deviam lá estar:
  * A base de dados local (backend/prisma/dev.db), que tem 33 utilizadores e 29
    lojas, com as passwords guardadas em hash.
  * Os ficheiros de log, e um deles tinha uma linha com as credenciais de
    demonstração escritas em texto normal, à vista de qualquer pessoa.
  * Um ficheiro ZIP, dumps de ficheiros e vários ficheiros de "árvore" antigos.

Porque isto era grave:
- A base de dados e os logs estavam PUBLICADOS no GitHub. As credenciais que
  apareciam no log (a conta de demonstração do owner) funcionam de verdade —
  o próprio script de testes do projecto faz login com elas. Ou seja, qualquer
  pessoa que visse o repositório podia entrar no sistema.

O que fiz:
1. Fiz um backup completo e verifiquei-o (cópia do repositório inteiro, cópia da
   base de dados e um "bundle" com todo o histórico). Confirmei que a cópia da
   base de dados era exactamente igual ao original (mesma impressão digital).
2. Usei a ferramenta oficial "git filter-repo" para APAGAR esses ficheiros de
   TODA a história do projecto (não só do último commit) — a base de dados, os
   logs, o ZIP, os dumps e os ficheiros de árvore.
3. Como este processo apaga a base de dados do disco, restaurei-a logo a seguir
   a partir do backup. Confirmei que ficou igual: 33 utilizadores e 29 lojas.
4. Reforcei o ficheiro .gitignore para que estes ficheiros NUNCA mais possam ser
   enviados para o GitHub (bases de dados .db, logs, ficheiros .zip).

Resultado:
- FUNCIONOU, e está provado: procurei cada um desses ficheiros em toda a história
  e todos dão "0 commits" — desapareceram. O repositório passou de 169 para 158
  ficheiros, e a raiz está limpa. Confirmei também que os ficheiros importantes
  (código, esquema da base de dados, scripts) NÃO foram apagados nem ignorados.
- Guardei tudo em segurança em: /home/kali/genesis-backup-20260918

O que AINDA NÃO está feito (importante):
- O envio para o GitHub (push) NÃO foi possível: o sistema pede autenticação da
  tua conta GitHub e eu não tenho acesso a ela. Enquanto não fizeres esse envio,
  as coisas más CONTINUAM visíveis no GitHub. Precisas de fazer:
      cd /home/kali/Genesis && git push --force-with-lease origin main
  (no terminal do VS Code, aceitando o pedido de autenticação do GitHub)
- ATENÇÃO: o envio tem de ser "force". Se alguém tiver outra cópia do projecto
  no computador, deve apagá-la e voltar a descarregar, senão volta a enviar o
  lixo antigo.

Problema NOVO e ainda mais grave que encontrei (precisa da tua decisão):
- As passwords de demonstração não estavam só nos logs. Estão escritas dentro do
  próprio código do backend (ficheiro backend/src/index.js), que é o sítio que
  CRIA essas contas: owner@genesis.local, cashier@genesis.local e
  admin@genesis.co.mz. Estão também repetidas em 6 scripts de teste, no test.sh
  e em dois ficheiros da pasta docs.
- Isto não se resolveu com a limpeza (continua no código actual) e não é uma
  simples limpeza: para corrigir bem, a aplicação deve ler essas passwords de um
  ficheiro de configuração secreto em vez de as ter escritas à vista, e isso
  muda a forma como fazes login na demonstração. Por isso parei aqui e não inventei
  uma solução sem falar contigo.

Outra coisa confirmada que continua por resolver:
- O painel de Super Admin ainda tem código dentro do site principal (o
  frontend/src/App.jsx importa o AdminLogin e o SuperAdmin/Dashboard, que tem 188
  linhas). A pasta admin-frontend/ já está bem feita e separada (porta 5175), mas
  falta apagar as cópias que estão dentro do site dos lojistas.

Ordem que sugiro para a próxima sessão:
1. Faz o push (comando acima) — é a prioridade número um.
2. Decide como queremos tratar as passwords escritas no código.
3. Apagar o código de admin do site principal (Fase 1).
4. Testar a sério o cancelamento de venda com PIN (foi mexido por outro agente
   sem prova nenhuma).

================================================================================
ACTUALIZAÇÃO DE 18 DE SETEMBRO DE 2026 (2.ª parte) — Passwords fora do código
================================================================================

O problema que encontrei (mais grave do que o lixo do repositório):
- O backend tinha um bloco de código que corria SEMPRE que o servidor arrancava
  (sem qualquer interruptor) e que criava três contas de demonstração — incluindo
  uma conta de SUPER ADMIN, a mais poderosa do sistema — com uma password escrita
  dentro do próprio código.
- Isto não era só um problema do computador local: no dia em que pusermos isto
  num servidor ligado à internet (que é o plano), qualquer pessoa que conhecesse
  aquele endereço e a password entrava como Super Admin e via os dados de TODOS
  os clientes. Era a porta da frente aberta.
- Encontrei também duas coisas que ninguém tinha visto:
  * O ecrã de login do site tinha as passwords JÁ PREENCHIDAS nas caixas —
    incluindo a do Super Admin. Bastava clicar em "Entrar".
  * Havia dois ficheiros "escondidos" do editor de código (os chamados ficheiros
    de swap, do Vim) que tinham sido enviados para o GitHub. Um deles continha
    uma versão antiga do código de login onde a password era comparada com um
    texto fixo escrito no código.
- Importante: confirmei que o código de login ACTUAL está correcto (usa
  comparação segura com bcrypt). O que estava mal era a versão antiga guardada
  naquele ficheiro escondido. Não há porta secreta activa.

O que fiz:
1. Transformei a criação das contas de demonstração num interruptor que está
   DESLIGADO por defeito: sem ligar esse interruptor, o sistema não cria contas
   nenhumas. Nunca será possível isto acontecer por acidente num servidor real.
2. As passwords da demonstração passaram para o ficheiro privado de configuração
   (.env), que nunca vai para o GitHub. O código deixou de as conter.
3. Tirei as passwords de TODOS os sítios: os 8 scripts de teste, o test.sh, os
   exemplos de comandos, os documentos, e os ficheiros do Postman.
4. Corrigi os ecrãs de login (principal e do painel de administração) para
   NUNCA virem com a password preenchida.
5. Apaguei os dois ficheiros escondidos do editor e passei a bloquear esse tipo
   de ficheiros, para não voltarem a entrar.
6. Limpei as passwords de TODO o histórico do projecto (não só da última
   versão), com a mesma técnica de antes, para que ninguém as possa encontrar
   olhando para versões antigas.

Resultado (com prova):
- FUNCIONOU. Verifiquei os 303 ficheiros guardados na base de dados do
  repositório — incluindo os binários e os "soltos" — e NENHUM contém já
  qualquer forma dessas passwords. Apenas restam textos de exemplo do tipo
  "<twilio-token>", que não são secretos.
- Testei o interruptor a sério: desligado → o servidor arranca sem criar contas;
  ligado → cria, e já NÃO mostra as passwords no registo de arranque; ligado sem
  a password definida → dá uma mensagem de erro clara em vez de criar algo
  inseguro.
- Testei o login real: funciona (resposta 200, perfil de gestor).
- A tua base de dados (33 utilizadores, 29 lojas) e o teu ficheiro .env
  sobreviveram intactos a toda a operação. Guardei cópias de segurança.
- O repositório ficou mais leve: de 11 MB para 3,5 MB.

O que DESCULPASTE que ainda falta (por ordem):
1. O ENVIO PARA O GITHUB CONTINUA POR FAZER — preciso da tua autenticação:
      cd /home/kali/Genesis && git push --force-with-lease origin main
   Enquanto não fizeres isto, a base de dados, os registos e as passwords
   continuam visíveis no GitHub. É a prioridade número um.
2. TROCAR AS PASSWORDS DE DEMONSTRAÇÃO. Como estiveram expostas, o correcto é
   mudá-las: altera no ficheiro backend/.env as três linhas que começam com
   DEMO_ (DEMO_OWNER_PASSWORD, DEMO_CASHIER_PASSWORD, DEMO_ADMIN_PASSWORD) e
   reinicia o backend. O sistema actualiza as contas sozinho no arranque.
3. Limpar o código de administração que ainda vive dentro do site principal
   (Fase 0-B.2) — a pasta admin-frontend/ já está bem feita e separada.
4. Testar a sério o cancelamento de venda com PIN (Fase 0-B.3).

Nota de método que quero registar (para não repetir erros):
- A minha primeira verificação dizia "0 ocorrências" e estava ERRADA. O comando
  que usei ignora ficheiros binários por defeito, e foi exactamente num ficheiro
  binário (o ficheiro escondido do editor) que estava o problema. Só descobri
  porque mudei de método e inspeccionei TODOS os ficheiros, um a um, sem
  filtros. Fica registado como regra: nas verificações de segurança, não se
  filtram ficheiros.

Data: 18 de Setembro de 2026 — Push e rotação das passwords (fim da Fase 0-B.1).
- O force-push que estava bloqueado foi feito com sucesso. O GitHub tem agora
  a história limpa, sem a base de dados, sem os logs e sem as passwords
  antigas. Confirmei que o repositório é privado e não tem cópias (forks),
  por isso o risco ficou limitado a quem já tivesse descarregado o código.
- As 3 passwords de demonstração foram trocadas por novas (20 caracteres
  cada). Provei na base de dados que as novas funcionam e que as antigas
  deixaram de funcionar para as 3 contas (dono, caixista e admin).
- Com isto, a Fase 0-B.1 está encerrada. O próximo passo é a Fase 0-B.2:
  tirar as páginas de admin (188 linhas) de dentro do site principal.

Data: 18 de Setembro de 2026 — Fase 0-B.2: separação do painel de admin.
- Apaguei as duas páginas de administração que viviam dentro do site dos
  lojistas (o login de admin e o painel com 188 linhas). Qualquer conta de
  Super Admin que tente entrar pelo site principal recebe agora uma
  mensagem a indicar o painel dedicado, em vez de ser enviada para uma
  página de admin dentro do site. As rotas /admin redireccionam para o
  login normal, e tirei também o botão "Super Admin" da barra lateral.
- Provei que resultou: o site compila sem erros e inspeccionei o ficheiro
  final que é servido aos lojistas — não contém nenhuma página de admin
  nem nenhuma chamada à API de administração. O painel separado
  (admin-frontend) compila bem e a API de admin no servidor não foi
  tocada.
- Falta fazer o commit e o push desta fase. Depois segue-se a Fase 0-B.3:
  testar a sério o cancelamento de venda com PIN.

  (Actualização: commit e push feitos com sucesso — confirmado no GitHub.)

Data: 18 de Setembro de 2026 — Fase 0-C.1: PC do balcão estacionado no Hub.
- O bug que reportaste (entrar como dono e clicar Vendas/POS voltar à Visão
  Geral) não era da senha nova. Era o guarda de acesso a funcionar como foi
  escrito: a barra lateral mostrava "Vendas / POS" a toda a gente, mas a
  rota só deixava entrar caixistas, por isso mandava o dono de volta.
- Mudei o comportamento: agora quem é caixista vê só "Vendas / POS" e entra
  directo; quem é dono vê "Caixistas (Hub do Balcão)" e é levado para o Hub,
  onde vai escolher o perfil do caixista com a senha antes de vender (o Hub
  completo, estilo Netflix, é a próxima fase). Também corrigi o link "Stock"
  que apontava para a página errada.
- Provei que resultou: o site compila, o login como dono funciona e a regra
  de decisão foi testada para os 4 casos (dono->Hub, caixista->POS, sem
  sessão->login, admin->login). O servidor de desenvolvimento já serve o
  código novo sem precisar reiniciar.
- Próximo: o Hub de Caixistas completo (criar caixista, entrar com senha,
  sair só com fecho de turno, voltar ao menu só com senha do dono).

Data: 18 de Setembro de 2026 — Fase 0-C.2: Hub de Caixistas pronto e provado.
- O Hub estilo Netflix está feito: o dono vê a grelha de perfis, clica
  "Entrar" e digita a senha daquele caixista; o POS abre com banner "A
  operar como X". Para sair do perfil, o sistema verifica se há vendas sem
  fecho de turno e bloqueia se houver. Para voltar ao menu do dono, pede a
  senha do dono. Os caixistas ficam sempre no Hub.
- Cada venda feita pelo dono em nome de um caixista fica gravada em nome do
  caixista (para os relatórios por vendedor), mas a auditoria regista que
  foi o dono quem operou. Provei com venda real: ficou em nome do vendedor
  certo. Provei também que um caixista não consegue forjar vendas em nome
  de outro (o sistema ignorou a tentativa e gravou em nome próprio).
- Também fechei uma falha de segurança: desactivar caixista agora só mexe
  no próprio estabelecimento (antes, um dono podia desactivar o caixista
  de outra loja). E o dono pode reactivar, trocar a senha do caixista e
  ver tudo na auditoria.
- Tudo limpo depois dos testes: vendas de teste apagadas, stock reposto,
  vendedor de teste desactivado. Site compila sem erros.
- Nota importante: a tua senha "Wendy1313$" deixou de funcionar porque ao
  reiniciar o servidor o sistema repôs a senha que está no ficheiro .env
  (da rotação que fizemos). Não é bug — usa a senha actual do .env, ou
  diz-me e trocamos de novo.
- Próximo: prova do cancelamento com PIN, ou a barra de meta em tempo real
  + relatório mensal em cascata (o lucro real com todas as deduções).

2026-09-18 — Password do Super Admin = Wendy1313$ (estavel em restarts)
O que foi feito:
- Mudaste a senha do owner@genesis.local pelo fluxo de reset (OTP). Mais tarde
  o login deixou de funcionar. Descobri porque: o backend, ao arrancar, corre o
  seed demo (SEED_DEMO_DATA=true) que faz UPSERT das contas demo (owner, cashier,
  admin) usando as passwords DEMO_*_PASSWORD do backend/.env — ou seja, a cada
  arranque as passwords dessas contas sao repostas e qualquer reset manual se perde.
- Corrigi alinhando o backend/.env: DEMO_ADMIN_PASSWORD e DEMO_OWNER_PASSWORD
  passaram a ser Wendy1313$. Assim o seed passa a garantir exactamente essa
  password em cada arranque, e deixa de brigar com resets manuais.
Porque: para teres login estavel de admin e owner sem surpresas.
Prova real:
- POST /api/auth/login admin@genesis.co.mz + Wendy1313$ -> 200 token
- POST /api/auth/login owner@genesis.local + Wendy1313$ -> 200 token
- GET /api/auth/me -> admin: super_admin, owner: owner, ambos activos
Nota importante: DEMO_CASHIER_PASSWORD (cashier@genesis.local) ficou com a
password aleatoria anterior no .env — se precisares do caixista demo, pede-me
que tambem a alinho. Contas reais (criadas pelo Owner) NAO sao tocadas pelo seed.

[2026-09-18] — CORRECAO FINAL DOS 3 BUGS DO HUB DE CAIXISTAS (Fase 0-C.2)

O que estava a acontecer: quando entravas no perfil de um caixista pelo Hub,
(a) a tua sessao de dono era TROCADA pela do caixista (porque o codigo fazia
login com a senha dele), (b) o fecho de turno ficava em teu nome em vez do
caixista (por isso o perfil dele nunca destravava), e (c) se o caixista
vendesse depois de fechar o turno, o sistema nao pedia fecho de novo.

O que fiz:
1. A entrada no perfil agora valida a senha do caixista SEM trocar de sessao
   (novo endpoint verify-password; o PC continua logado como dono).
2. O fecho de turno, feito pelo dono no modo Hub, fica em nome do caixista
   cujo perfil esta activo. O caixista nao consegue fechar em nome de outro.
3. Vender depois de fechar o turno volta a exigir novo fecho para sair.
4. BONUS descoberto ao testar: o registo de auditoria do fecho de turno
   estava PARTIDO desde o principio (erro de tipo no campo new_value) — o
   sistema gravava o fecho mas respondia erro 500 e nao registava auditoria.
   Corrigido.

Funcionou? SIM — prova: teste automatico real contra o backend a correr,
16 de 16 verificacoes PASSARAM, incluindo: senha errada e certa, a sessao
do dono intacta depois de entrar no perfil, venda em nome do caixista,
turno aberto/fechado nos momentos certos, e bloqueio de forja. Depois disso
limpei ficheiros temporarios e uma base de dados orfa que estava a causar
confusao. Commit e push feitos.

================================================================================
[DATA: 19-09-2026] — LOTE A: a pagina do Hub e o POS, agora a serio
================================================================================

O QUE FIZ

1. O botao "Caixistas (Hub do Balcao)" agora abre a pagina dedicada /hub
   (aquela de escolher perfil, estilo Netflix), e nao mais aquela tabela feia.
   A pagina feia foi APAGADA do sistema.

2. O POS deixou de ser "uma aba dentro do painel do dono". Agora e um ecra
   proprio, sem o menu lateral (Visao Geral, Stock, etc.). Isso era o motivo
   pelo qual o caixista conseguia clicar em tudo e ir ver o painel do dono.

3. O BLOQUEIO agora bloqueia mesmo. Antes ele nao bloqueava nada:
   - o aviso de bloqueio aparecia POR CIMA do POS, com os produtos todos
     clicaveis por baixo;
   - so aparecia se o caixista carregasse em "Fechar turno";
   - tinha um botao "Voltar ao Hub" que deixava sair do bloqueio;
   - e o servidor aceitava vendas mesmo com o perfil bloqueado.
   Agora: assim que entra no POS, o sistema verifica se o perfil esta
   bloqueado. Se estiver, o ecra fica TODO tapado, sem nenhum botao de sair,
   e so a tua senha (senha do dono) o desbloqueia.

4. O caixista bloqueado continua a nao conseguir VENDER — o servidor recusa
   a venda (erro 403) enquanto o perfil estiver bloqueado. Esta e a parte que
   o fundador reportou que estava a falhar.

5. Na pagina /hub, um perfil bloqueado aparece com o selo "BLOQUEADO" e a
   contagem de erros. Se clicares nele, pede logo a TUA senha e desbloqueia-o.

6. Podes criar caixistas na propria pagina do Hub, com o botao "+ Novo
   caixista" — e so depois de pores a tua senha de dono.

7. Removi o "valor esperado" fantasma. Aquilo era codigo morto: NUNCA era
   mostrado. O valor que estava a subir enquanto se adicionava produto ao
   carrinho era o TROCO, calculado a cada produto. O fecho de turno
   continua cego: o sistema nao revela quanto espera.

8. Tambem corrigi a lista de caixistas que estava a enviar para o browser a
   password encriptada dos caixistas. Agora so vai o necessario (nome,
   contacto, estado, bloqueio).

9. O nome "Sergio Bila" que aparecia fixo no canto do POS foi removido
   (era dado de demonstracao enganador). Agora mostra o nome real de quem
   esta a operar.

10. O menu lateral do dono foi reposto como deve ser, com Produtos,
    Fornecedores, Trabalhadores, Chenecas, Relatorios, Metas, Definicoes e
    Chaves de Dispositivo. Havia tambem um botao "Chaves de Dispositivo"
    que nao abria nada (faltava a rota) — tambem foi corrigido.

PORQUE
Porque o fundador testou e mostrou em imagens que: o caixista continuava a
vender depois de bloqueado, conseguia sair do bloqueio sozinho, conseguia
entrar no painel do dono, e o botao do Hub abria uma pagina mal feita.

O QUE ISSO RESOLVEU (com prova real)
Corri um teste automatico contra o servidor a funcionar (nada de "parece
que funciona"):
  - entrar no sistema como dono             -> OK
  - criar caixista                          -> OK
  - vender em dinheiro em nome dele         -> OK
  - errar o fecho 3 vezes                   -> 1a e 2a sem bloqueio, 3a BLOQUEIA
  - a pagina do Hub mostra BLOQUEADO        -> OK (3 erros)
  - TENTAR VENDER BLOQUEADO                 -> RECUSADO pelo servidor (403)
  - desbloquear com a senha do dono         -> OK
  - vender outra vez                        -> ACEITE
Resultado: 13 testes passados, 0 falhados. As paginas 5173, 5175 e 4000
respondem 200 e o programa compila sem erros.

FUNCIONOU? Sim — provado pelos 13 testes acima.

FALTA AINDA (proximos lotes, ja combinados)
  - Lote B: opcao de DESCONTO no caixa (manual e regras com condicoes, ex:
    "terca-feira" ou "mais de 3 arroz"), e o RECIBO bonito com nome da loja,
    localizacao, nome do caixista e QR real, com o nome do ficheiro
    "Venda1-19-09-2026+00:21:53" (o contador volta a 1 quando muda o dia).
  - Lote C: avisos para o dono (sino) quando o caixista erra/queda bloqueado
    ou faz fecho correcto; e ver/reimprimir recibos antigos so com a tua senha.
  - Lote D: o visual de todas as interfaces (a Visao Geral e as restantes
    abas), ao estilo CRM profissional, com graficos e popups bonitos.

================================================================================
[DATA: 19-09-2026] — LOTE D1: a base visual e o nome verdadeiro da loja
================================================================================

O QUE FIZ

1. Criei a FUNDACAO visual que faltava. Antes, cada pagina escrevia as cores
a mao e havia 48 sitios com cartoes BRANCOS no meio de um fundo escuro. Isso
era a razao de parecer "feito por amador": era o estilo de demonstracao do
Tailwind misturado com o tema escuro. Agora ha uma paleta unica (tokens) com
o preto profundo + vermelho Genesis que escolheste, e VERDE = dinheiro,
VERMELHO = alerta, AZUL = meta.

2. Criei pecas de interface reutilizaveis: cartoes, cartoes de numeros (KPI),
   botoes, etiquetas, campos, JANELAS FLUTUANTES (popups) com animacao,
avisos que aparecem no canto (toasts), tabelas, estados vazios, esqueletos
de carregamento e a BARRA DE METAS com a barra de bonus azul por cima dos
100%.

3. CORRIGI UM PROBLEMA GRAVE QUE NAO SABIAMOS: a barra lateral mostrava o
   nome de uma loja FALSA ("Bottle Store Motla"). Esse nome vinha de um
ficheiro de dados de demonstracao, nunca existiu na tua base de dados. As
tuas lojas reais chamam-se "Loja Teste", "Genesis Demo Store", etc. Agora a
barra lateral vai buscar o nome VERDADEIRO a base de dados.

4. CORRIGI OUTRO PROBLEMA: NAO HAVIA BOTAO DE SAIR. O servidor tinha a
   funcao de terminar sessao, mas nenhum ecra a chamava. Agora tens o botao
   de sair no fundo da barra lateral.

5. A pesquisa "Ctrl+K" agora funciona a serio: abre uma janela flutuante
   onde escreves e salta directamente para a pagina (produtos, metas,
chenecas, relatorios...).

6. Apaguei codigo morto: uma pagina de POS dentro da pasta do DONO (que nem
   estava ligada a nada, e o dono nao deve ter POS) e dois componentes que
   so serviam essa pagina.

PORQUE
Porque o fundador mostrou imagens e disse claramente que a Visao Geral e as
outras abas estavam "com textos todos colados, uma salada, nada separado,
cores feias". A raiz era nao existir uma base visual partilhada.

O QUE ISSO RESOLVEU (com prova real)
  - O novo endereco do nome da loja foi testado: 5 testes passados, 0 falhados.
    O nome devolvido e "Genesis Demo Store" (real) e confirmou-se que NAO e
    o falso "Bottle Store Motla".
  - A base de dados nao expoe o PIN secreto do dono no perfil da loja.
  - O programa compila sem erros; a paleta e as pecas novas estao no pacote
    final; o botao de sair esta no pacote final.
  - As paginas 4000, 5173 e 5175 respondem.

FUNCIONOU? Sim — provado pelos testes acima.

FALTA AINDA NO LOTE D
  - D2: refazer a VISAO GERAL (o ecra que mais incomodou) com cartoes de
    numeros, graficos a serio (receita por dia, produtos mais vendidos,
    formas de pagamento, barra da meta) e separar aquele ficheiro gigante
    de 696 linhas que tem tudo misturado.
  - D3: aplicar o mesmo visual aos restantes ecras (Produtos, Stock,
    Fornecedores, Trabalhadores, Chenecas, Metas, Definicoes, Relatorios...)
    e o relatorio mensal em forma de cascata (receita -> descontos -> LUCRO
    REAL). Restam 46 sitios com cartoes brancos a corrigir.
  - D4: polir o Hub e o POS (efeitos, hover, popups).
Depois disto: Lote B (desconto + recibo bonito + numeracao diaria) e Lote C
(avisos para o dono + reimprimir recibos antigos).

================================================================================
[DATA: 24-09-2026] — LOTE D2 (PARTE VISUAL): o produto ficou com cara de
aplicacao a serio
================================================================================

O QUE FIZ

1. Criei o MOTOR DE MOVIMENTO do produto. Antes o Genesis era uma interface
   parada. Agora tem coisas com Personality, mas sem ser um circenco:
   - O RECIBO, depois de uma venda, sai da impressora em 3D (a folha sai do
     slot com perspectiva e assenta). E o momento que o cliente ve.
   - O brilho dos cartoes e cartoes de numeros SEGUE O RATO.
   - O fundo tem auroras muito lentas e uma grelha que deriva devagar.
   Se a pessoa pediu menos movimento no sistema, tudo isto desliga-se sozinho.

2. REFAZI O ECRÃ DE ENTRAR. Antes era um formulario com um cadeado gigante
   desenhado a mao no meio da tela — o aspecto de "aplicacao de exemplo".
   Agora: a esquerda explica o produto (com 3 pontos de valor e a garantia de
   seguranca) e a direita tem um cartao de vidro. Em telefone o painel some e
   fica so o cartao, bem arrumado.

3. REFAZI O ECRÃ DE PERFIL BLOQUEADO E O DE FECHO DE TURNO. Tinham cores
   escritas directamente no codigo (dezenas de linhas) e emojis. Passaram a
   cartoes limpos com icones e as mesmas regras de cor do resto do programa.

4. TIREI TODOS OS EMOJIS DO PROGRAMA. Eram coisas como um emoji de dinheiro, um
   de cadeado, um de grafico, uma caixa.troca. Troquei tudo por icones com a
   mesma linha visual em toda a aplicacao — 12 ecras do dono, mais a Visao
   Geral e o Caixa. Varri os 185 ficheiros: sobrou ZERO emoji.

5. MUDEI AS CORES DOS GRAFICOS. Antes estavam escritas a mao no ficheiro do
   grafico. Agora dizem "a cor da marca" ou "a cor de alerta", como todo o
   resto. Se um dia mudares a paleta, os graficos mudam sozinhos.

6. TORNEI OS CARTOES DE NUMEROS REACTIVOS AO RATO — em TODAS as paginas do
   dono, nao so na Visao Geral, porque o efeito ficou noimo que e partilhado.

O PROBLEMA GRAVE QUE ENCONTREI (e que nao era so visual)

No ecra do Caixa, a linha do DESCONTO estava errada de duas maneiras:
  - escrevia um codigo invalido que fazia o navegador ignorar a cor;
  - e subtraiava os numeros ao contrario, pelo que aparecia
    "MZN -50,00" em vez de "MZN 50,00" quando tinhas dado 50 de desconto.
Numa aplicacao que mexe com dinheiro, um sinal negativo no sitio errado
faz o cliente desconfiar do valor. Esta corrigido. O calculo do total, do
troco e do que vai para a base de dados NAO foi mexido.

TAMBEM CORRIGI UM ERRO QUE EU PROPRIO TINHA FEITO

Reparei que, ao por o brilho do rato no cartao de numeros, eu tinha
apagado a barrinha colorida de cima do cartao (os dois usavam a mesma
peca invisivel do CSS). Detectei antes de entregar, troquei o brilho para
uma peca propria e a barrinha voltou. Regra: o programador tem de verificar
ate as coisas que ele proprio estragou.

O QUE ISSO RESOLVEU (com prova real)

  - O programa compila sem erros: "built in 5.95s", codigo de saida 0.
  - Varredura automatica aos 185 ficheiros: 0 emojis, 0 cores escritas a mao
    no codigo dos ecras que toquei.
  - O bug do desconto foi revisto linha a linha no codigo.

LIMITE HONESTO DESTA VEZ

So provei que COMPILA e que o codigo esta certo. Nao abri o programa no
navegador a ver as cores e as animacoes a correr. Isso fica para a
verificacao visual, que e o passo seguinte.

IMPORTANTE (O QUE NAO MEXI)

Nao toquei em nenhuma regra de dinheiro, nem na separacao de dados entre
lojas (cada loja so ve a sua), nem nas regras de quem pode ver o que, nem
no comportamento quando a internet falha. Isto foi so aparencia, mais a
correccao da linha do desconto.

FALTA AINDA
  - Abrir no navegador e confirmar o aspecto ecra a ecra.
  - Polir a barra lateral e o Hub.
  - O mapa 3D do sistema (ficheiro que ainda nao existe).
  - Lote B (recibo com nome da loja e numeracao diaria) e Lote C (avisos
    para o dono e reimprimir recibos antigos).

